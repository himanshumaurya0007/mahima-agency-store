PORT=5000

NODE_ENV=development        # or production / test
PORT=5000
CORS_ORIGIN=http://localhost:5173   # your frontend URL (adjust for production)

# Environment
NODE_ENV=development
# NODE_ENV=production
PORT=5000
CORS_ORIGIN=http://localhost:5173

# Database
DB_NAME=mahima_agency_store
# MONGO_URI=mongodb+srv://<db_username>:<db_password>@mas-cluster.kxa6ggt.mongodb.net/

# Auth
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=mahimaAgencyStore
ACCESS_TOKEN_EXPIRES_IN=1h
REFRESH_TOKEN_SECRET=MahimaAgencyStore
REFRESH_TOKEN_EXPIRES_IN=7d
BCRYPT_SALT_ROUNDS=10

# Logging
# LOG_LEVEL=info
# LOG_DIR=logs

# Email
# SMTP_HOST=<smtp_host>
# SMTP_PORT=<smtp_port>
# SMTP_USER=<smtp_user>
# SMTP_PASS=<smtp_pass>
# EMAIL_FROM=<email_address>